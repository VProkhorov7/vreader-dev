---
paths:
  - "**/*"
---

# Git Rules

- Check `git status` before broad changes.
- Never change remote URLs unless explicitly asked.
- Never force-push unless explicitly asked.
- Keep separate repositories separate.
